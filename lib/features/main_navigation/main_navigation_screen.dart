import 'package:flutter/material.dart';

class MainNavigaionScreen extends StatelessWidget {
  const MainNavigaionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
